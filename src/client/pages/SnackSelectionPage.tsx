import {Typography} from '@mui/material';
import * as React from 'react';

const SnackSelectionPage = () => (
  <Typography variant={'h4'}>
    This is the snack selection page.
  </Typography>
);

export default SnackSelectionPage;
